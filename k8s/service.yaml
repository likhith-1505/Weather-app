apiVersion: v1
kind: Service
metadata:
  name: weather-devops-service
spec:
  type: NodePort
  selector:
    app: weather-devops-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
